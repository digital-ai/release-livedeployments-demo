# Kubernetes for release setup

Sets up Kubernetes for Release.

```yaml instacli
Confirm: Set up Kubernetes for Release?
      
Run script: configure-release.cli.md

On error:
  Print: Kubernetes for Release not installed
```
